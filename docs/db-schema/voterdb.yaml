#collection : user
user:
  username: string
  email: string
  password: string
  fullname: string
  smPhoto: bytes
  lgPhoto: bytes
  location: string
  joinedDate: date
  voteCounter:
    votes: int
    voted: int
    starred: int

#collection : vote
vote:
  title: string
  creator: object(user)
  createTime: date
  voted: int
  starred: int
  isMulti: boolean
  isDel: boolean
  delTime: date
  choices:
    - no: int
      content: string
      voted: int
      ratio: float

#collection : notification
notification:
  to: object(user)
  type: int #0:sys
  notiTime: date
  isReaded: boolean
  content: string
  isDel: boolean
  delTime: date

#collection : voted
voted:
  voter: object(user)
  vote: object(vote)
  voteTime: date

#collection : starred
starred:
  voter: object(user)
  vote: object(vote)
  starredTime: date









